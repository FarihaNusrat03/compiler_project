#include<iostream>

int main()
{
    int i;
    int sum;
    bool flag;

    i = 1;
    sum = 0;
    flag = true;

    while(i <= 5)
    {
        if(flag && i < 4)
        {
            sum = sum + i;
        }
        else
        {
            sum = sum + 1;
        }

        i = i + 1;
    }

    print sum;

    return sum;
}